import Hero from "@/features/(client)/list/Hero";

const List = () => {
    return (
        <section className="list w-full overflow-hidden">
            <Hero />
        </section>
    );
};

export default List;