import { JSX } from "react";

const Filter = (): JSX.Element => {
    return (
        <section className="filter w-full overflow-hidden">
            
        </section>
    );
};

export default Filter;